const ImageUtil = {};

/**
 * 소스 이미지를 받아 프로젝트 내부에서 사용할 이미지로 변환해주는 메소드
 *
 * @param {Image} source
 * @returns {Image}
 */
ImageUtil.prepare = function (source) {
  const result = {};

  return result;
};

Object.freeze(ImageUtil);
export default ImageUtil;
